{
  "name": "cloudflare-ikuai",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --minify=false",
    "clean": "rimraf dist",
    "copy-assets": "copyfiles -u 1 assets/**/* dist/assets/",
    "release": "npm run clean && npm run build && npm run copy-assets",
    "start": "npm run release && node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "csv-parser": "^3.2.0",
    "iconv-lite": "^0.6.3",
    "ikuai": "^1.3.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3",
    "copyfiles": "^2.4.1",
    "esbuild": "^0.25.2",
    "rimraf": "^6.0.1"
  }
}
